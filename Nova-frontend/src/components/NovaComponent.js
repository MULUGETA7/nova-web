import React from 'react';

const NovaComponent = () => {
    return (
        <svg width="100%" height="100%">
            <rect x={475} y={333} width={180} height={38} fill="#180123" opacity="1" />
            <text x={570} y={355} fill="url(#textGradient)" fontSize="32" textAnchor="middle" dominantBaseline="middle">
                NOVALABS
            </text>
        </svg>
    );
};

export default NovaComponent;